{
  "name": "issues2json",
  "version": "1.0.0",
  "description": "Generate JSON from GitHub issues",
  "scripts": {
    "build": "postcss src/*.css -d dist",
    "prepare": "npm run build"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "cssnano": "^7.0.7",
    "postcss": "^8.5.6",
    "postcss-cli": "^11.0.1"
  }
}
